from ship import Ship

class Arsenal:
    names = 'cruiser destroyer submarine battleship carrier'
    sizes = [3,2,3,4,5]
    def __init__(self):
        self.ships = build_arsenal[{'Name':'destroyer'}, {'submarine', 'cruiser battleship', 'carrier']

    def build_arsenal(self):
        ships = [Ship(name, size) for name in names for size ]

    def hit():  
